#!/bin/bash
# ─────────────────────────────────────────────────────────────────────────────
# WAI THIN WRAPPER — wai-exit.sh
# Delegates to: /home/mario/projects/basher/wai-exit.sh  (canonical)
# Running from: $PWD at call time — project context is preserved automatically
# To update:    edit basher/wai-exit.sh — all projects pick it up instantly
# ─────────────────────────────────────────────────────────────────────────────
exec "/home/mario/projects/basher/wai-exit.sh"
