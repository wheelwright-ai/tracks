# Shared

Framework assets used by both spokes and hubs.

Current public ownership:

- `codebase/skills/` for reusable framework skills
- `codebase/tools/` for framework utilities once generalized
- `teachings/` for shared framework teachings

This structure is compatibility-first. Legacy copies remain elsewhere in the repo
until tests and scripts are updated to consume these paths directly.
